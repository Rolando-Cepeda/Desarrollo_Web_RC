# Instalación de NODE.
	## https://nodejs.org/en/
	## Le damos Next, hasta que se instale.
# REACT
	## CONCEPTOS.- es una librería de JavaScript de código abierto
	## Implementamos una sola vez nuestro archivo y la podemos utilizar en varias partes.
	## COMPONENTES.- Son pequeños elementos (piezas de código).
# VIRTUAL damos
	## React añade el virtual DOM, SOLO los componentes que cambian se CARGARÁN.
# Vamos a cmd: lo crearemos en el escritorio:
	## La carpeta en donde lo crearemos se llamará client.
	## C:\Users\Tardes>cd Desktop
	## C:\Users\Tardes\Desktop>
	## C:\Users\Tardes\Desktop>    npx create-react-app client
	## Se deberá instalar, cuando nos salga lo siguiente:
		### Happy hacking!
		### C:\Users\Tardes\Desktop>
	## Entramos dentro de client: C:\Users\Tardes\Desktop>cd client
									C:\Users\Tardes\Desktop\client>

	## C:\Users\Tardes\Desktop\client>code .

# DEPENDENCIAS.- Librerias o paquetes que necesitan los elementos en REACT.
# Como arrancar un proyecto de REACT:
	## cd client
  	## npm start
# Abrimos la consola en VS: npm start
# Si sale error debemos instalat como administrador en POWER SHELL
	## Set-ExecutionPolicy Unrestricted
# En VSC ejecutamos otra vez npm start y ya tendremos el localhost de React:localhost:3000
	## Para cerrar la terminal que esta trabajando con REACT escrimos: control c
# Fichero gitignore.- Ponemos la ruta relativa o completa de los directorios completos que queremos que se IGNOREN
# SRC: todos los ficheros estan programados con JS y XML
	## index.js.- RENDER = es igual a pintar.
	## APP.js.- Aquí van los componentes

# JSX.- XML.- permite escribir lenguajes de marca(dentro de las cuáles podemos declarar JS)
# Hemos clonado el proyecto de REACT holamundo-react en escritorio.
# Abrimos el proyecto en la terminal de windows:
	## C:\Users\Tardes\Desktop>cd holamundo-react
	## C:\Users\Tardes\Desktop\holamundo-react>code .
	## C:\Users\Tardes\Desktop\holamundo-react>
## En la terminal de VSC descargamos las dependencias: npm install o npm i
## Luego para iniciarlo: npm start


